spring:
  profiles:
    include:
      - notification-prod
  jpa:
    properties:
      hibernate:
        ddl-auto: validate
        show_sql: false
        format_sql: false

jwt:
  expiration-minutes: 15

nginx:
  require-proxy: true

chirp:
  rate-limit:
    ip:
      apply-limit: true
