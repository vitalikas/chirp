spring:
  jpa:
    properties:
      hibernate:
        ddl-auto: update
        show_sql: true
        format_sql: true

jwt:
  expiration-minutes: 1000

nginx:
  require-proxy: false

chirp:
  rate-limit:
    ip:
      apply-limit: true
